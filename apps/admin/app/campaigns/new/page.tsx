import CampaignEditorClient from "../[id]/CampaignEditorClient";

export const dynamic = "force-dynamic";

export default function NewCampaignPage() {
  return <CampaignEditorClient campaignId={null} />;
}
